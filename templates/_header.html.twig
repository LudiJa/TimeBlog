<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3 container-fluid">
         <a class="navbar-brand" href={{ path('app_home') }}>Navbar</a>
         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active"><a class="nav-link" href={{ path('app_home') }}>Home</a></li>
                <li class="nav-item"><a class="nav-link" href={{ path('app_post_create') }}>Create a post</a></li>
            </ul>
        </div>

        <div class="justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item active"><a class="nav-link" href={{ path('app_admin') }}>Admin</a></li>
                {% endif %}
                <li class="nav-item active"><a class="nav-link" href={{ path('app_logout') }}>Logout</a></li>
            {% else %}
                <li class="nav-item active"><a class="nav-link" href={{ path('app_register') }}>Sign up</a></li>
                <li class="nav-item active"><a class="nav-link" href={{ path('app_login') }}>Login</a></li>
            {% endif %}
            </ul>
        </div>
    </nav>
</header>